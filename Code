/* Working method */
.tinted-image {
  background: 
    /* top, transparent red, faked with gradient */ 
    linear-gradient(
      rgba(242, 115, 44, 0.65), 
      rgba(242, 115, 44, 0.65)
    ),
    /* bottom, image */
    url(http://imageURLhere);
}
